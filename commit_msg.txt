v1.2.40: Ensure local data loads properly on install

### Fix
- Ensured all local data files (mangayomi.json, zangetsu.json, simplstream.json) are loaded on startup
- The load functions (loadLocalMangayomi, loadLocalZangetsu, loadLocalSimplStream) are now properly called in load()
- All data files in /public/data/ are included in the build

### New Sources (All Working Offline)
- **Mangayomi** - 16 manga/novels with covers, scores, genres
- **Zangetsu** - 12 anime with posters, scores, status  
- **SimplStream** - 6 torrent sources (anime, series, movies)

### All Sources Working Offline
No external API dependencies - all data loads from local JSON files in `/public/data/`

### Installation
Download and run `MFY Setup 1.2.40.exe` to install. The app will auto-update on next launch.